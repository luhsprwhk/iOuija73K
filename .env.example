# Anthropic Claude API Key (for local development with Express proxy server)
# Get your API key from: https://console.anthropic.com/
#
# LOCAL DEVELOPMENT:
# - Set VITE_CLAUDE_API_KEY here and run `npm run server` to use the Express proxy
# - OR run `npm run dev:all` to start both dev server and proxy server together
#
# PRODUCTION (Netlify):
# - Set CLAUDE_API_KEY (without VITE_ prefix) in Netlify Environment Variables
# - Go to: Site settings > Environment variables > Add a variable
# - The Netlify Function will automatically use this key securely server-side
VITE_CLAUDE_API_KEY=your_api_key_here

# Optional: Override the Claude API proxy URL
# Leave commented to use automatic detection (Netlify Functions in prod, localhost in dev)
# VITE_CLAUDE_PROXY_URL=http://localhost:3001/api/claude
